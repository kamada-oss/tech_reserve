= render 'shared/header'
.top-main
  .top-main__image
  .top-main__text
    %h1 日々の学習をアウトプット
    %h2 プログラミング学習の内容を誰かと共有したり、
    %h2 積み上げを視覚的に楽しむサービスです。

    - unless user_signed_in?
      .top-main__signup
        = link_to new_user_session_path, class: 'btn-guest' do
          ログイン
        = link_to new_user_registration_path, class: 'btn-registration' do
          ユーザー新規登録
    - else
      .top-main__signup
        = link_to categories_index_path, class: 'btn-guest' do
          はじめる
    .top-main__guest
      %p
        ポートフォリオ用のゲストユーザーのログインは
        %span
          = link_to home_test_path, class: '' do
            こちら
= render 'shared/footer'