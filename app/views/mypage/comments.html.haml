= render 'shared/header'
.mypage
  = render 'side_bar'
  .mypage__comments
    .mypage-form__header
      投稿一覧
    .index-main__comments
      - if @comments.present?
        = render 'categories/comment'
      - else
        .mypage__nothing
          投稿はまだありません。
  